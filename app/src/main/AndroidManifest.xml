<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.exfolyart.myfuckinapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rh_launcher3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rh_launcher3_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFuckinApp">
        <activity android:name=".MainActivity2"
                  android:label="@string/goto3"></activity>
        <activity android:name=".Exfolyart"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>